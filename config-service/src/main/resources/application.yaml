server:
  port: 8079
spring:
  cloud:
    config:
      server:
        git:
          default-label: eureka
          uri: https://github.com/FranciscoJesusMC/microservicios
          search-paths: config-data
  application:
    name: config-service          